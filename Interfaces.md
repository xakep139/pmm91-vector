# Интерфейсы #
`IFigure` наследуется от:
  * `IGeometryObject`
  * `IGraphicObject`
Остальные:
  * `ICommand`
  * `ICommandStack`
  * `IFileManager`
  * `IFigureMover`
  * `IFigureCollection`
  * `IGraphics`